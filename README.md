# Invitations

Tasks:

```shell
npx hardhat help
npx hardhat node
npx hardhat deploy
npx hardhat [admin]-invite --network localhost --invitee ... --from ...
npx hardhat get-inviter --network localhost --address ...
npx hardhat get-invitees --network localhost --address ...
```
